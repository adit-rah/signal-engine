# Analysis package marker. The scripts in this directory are designed to be
# invoked standalone via `uv run python bin/scripts/analysis/<name>.py`,
# but having __init__.py allows them to be imported as a package if needed.
